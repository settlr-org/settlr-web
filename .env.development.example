# Local development — web talks to local API via proxy
# For `npm run dev` with `docker-compose.local.yml` (api at localhost:18080)
NEXT_PUBLIC_API_URL=http://localhost:18080
API_URL=http://localhost:18080
