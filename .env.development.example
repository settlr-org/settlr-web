# Local development — web talks to local API via proxy
# For `npm run dev` with `docker-compose.local.yml` (api at localhost:18081)
NEXT_PUBLIC_API_URL=http://localhost:18081
API_URL=http://localhost:18081
